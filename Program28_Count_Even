using System;
class Program {
    static void Main() {
        int[] a = {1,2,3,4};
        int count = 0;
        foreach (int x in a)
            if (x % 2 == 0) count++;
        Console.WriteLine(count);
    }
}
