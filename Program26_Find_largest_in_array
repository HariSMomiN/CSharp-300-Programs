using System;
class Program {
    static void Main() {
        int[] a = {5, 9, 2};
        int max = a[0];
        foreach (int x in a)
            if (x > max) max = x;
        Console.WriteLine(max);
    }
}
